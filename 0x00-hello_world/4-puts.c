#include <stdio.h>
/*
  -->The main() function is the first function in your program that is executed when it begins executing
  --> The puts function in C is used to write a line or string to the stdout that is up to, but does not include, the null character
 */

int main(void)
{
  puts("\"Programming is like building a multilingual puzzle");
  return (0);
}
